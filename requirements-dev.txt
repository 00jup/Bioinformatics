# 개발 / 데이터 재생성 전용 의존성
# 일반 사용자는 requirements.txt 만 설치하면 됨.
#
# 이 파일이 필요한 경우:
#   - TDC에서 DILI 데이터 새로 받기 (src/data_preparation.py)
#   - 개발 (lint, test)
#
# 설치:
#   pip install -r requirements-dev.txt

-r requirements.txt

# 데이터 재다운로드용 (PyTDC가 옛 sklearn==1.2.2 핀 강제 → Python 3.13 호환 안 됨)
# 데이터 새로 받을 때만 깔고 평소엔 빼는 게 좋음
PyTDC>=0.4.0

# 개발 도구
ruff
pytest>=7.4.0
pytest-mock>=3.11.0
